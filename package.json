{
  "name": "reverse-cyritex",
  "version": "1.0.0",
  "description": "Cloudflare Worker HTTPS reverse proxy for routing JP VPS traffic to China target server",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step required'",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}